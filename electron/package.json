{
  "name": "electron",
  "version": "0.1.0",
  "description": "",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "cd ../client && npm run build && cp -r ./build ../electron/dist"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "electron": "^23.1.1"
  }
}
